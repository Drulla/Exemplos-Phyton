lista_simples_inteiro = [1,2,3,8,14,5,7]

meu_iter = iter(lista_simples_inteiro)

#for implementado em python
while True:
    try:
        elemento = next(meu_iter)
        print(elemento)
    except StopIteration:
        break
